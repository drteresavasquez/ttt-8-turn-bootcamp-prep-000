#!/usr/bin/env ruby

require_relative '../lib/turn'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"
display_board(board)
turn(board)
input = gets.chomp
index = input_to_index(input)
move(board, 4, "X")
display_board(board)
turn(board)
input = gets.chomp
